# キッチンカー注文管理アプリ

スマートフォンで使えるキッチンカー向け注文管理Webアプリケーションです。

> 📅 **最終更新**: 2026年1月9日

## 実装状況サマリー

| カテゴリ | 実装済み | 未実装 | 進捗率 |
|----------|----------|--------|--------|
| バックエンドAPI | 8/8 | 0 | ✅ 100% |
| フロントエンドページ | 2/2 | 0 | ✅ 100% |
| 主要機能 | 7/9 | 2 | 🔶 78% |
| UI/UX機能 | 10/17 | 7 | 🟡 59% |
| 認証機能 | 0/1 | 1 | 🔴 0% |

---

## 機能

- 📝 注文の入力・管理
- ⏰ 注文タイムスタンプの記録
- 🍔 商品の自由な編集
- 📦 複数商品の追加
- 🖼️ 商品画像のアップロード・表示
- 📱 スマートフォン対応レスポンシブUI
- 🔐 店舗ごとの認証機能（マルチテナント対応）
- 💰 売上計算機能

---

## 実装状況詳細

### バックエンドAPI（Flask）

| エンドポイント | メソッド | 説明 | 状態 |
|----------------|----------|------|------|
| `/api/orders` | GET | 注文一覧取得 | ✅ 実装済み |
| `/api/orders` | POST | 新規注文作成 | ✅ 実装済み |
| `/api/orders/<id>` | PUT | 注文更新 | ✅ 実装済み |
| `/api/orders/<id>` | DELETE | 注文削除 | ✅ 実装済み |
| `/api/products` | GET | 商品一覧取得 | ✅ 実装済み |
| `/api/products` | POST | 新規商品作成 | ✅ 実装済み |
| `/api/products/<id>` | PUT | 商品更新 | ✅ 実装済み |
| `/api/products/<id>` | DELETE | 商品削除 | ✅ 実装済み |
| `/api/upload` | POST | 画像アップロード | ✅ 実装済み |
| `/api/sales/today` | GET | 当日売上取得 | ✅ 実装済み |
| `/api/health` | GET | ヘルスチェック | ✅ 実装済み |

### フロントエンド（React + TypeScript）

#### 実装状況（frontend/src から確認した現状）

- **Pages**
    - `src/pages/Orders.tsx` → `components/pos/POSScreen.tsx`（POSレジ画面、商品一覧・カテゴリ・カート・発注フロー実装）
    - `src/pages/Products.tsx`（商品管理画面、商品一覧・追加・編集・削除のUIとAPI連携実装）

- **主なコンポーネント（実装済み）**
    - `components/pos/POSScreen.tsx`：カテゴリタブ、商品一覧、カート操作（数量増減）、合計金額表示、発注ボタン、スワイプで売上画面遷移などが実装されています。
    - `components/pos/CartPreview.tsx`：カートのプレビュー・編集・全削除・発注ボタンを持つポップアップが実装されています。
    - `components/pos/OrderConfirmPopup.tsx`：発注完了時の確認ポップアップが実装されています。
    - `components/pos/OrdersListPopup.tsx`：本日の注文一覧とステータス管理用ポップアップが実装されています。
    - `components/products/ProductForm.tsx`：商品作成／編集フォーム（画像アップロード連携含む）が実装されています。
    - `components/sales/TodaySales.tsx`：当日の売上サマリー表示が実装されています。
    - `components/orders/OrderCard.tsx`、`components/orders/NewOrderForm.tsx`（NewOrderForm は非推奨扱いだがファイルは存在します）
    - `components/ui/*`：`button.tsx`, `card.tsx`, `input.tsx`, `badge.tsx`, `label.tsx`, `tabs.tsx`, `textarea.tsx` 等の共通UIコンポーネントが揃っています。

- **API クライアント**
    - `src/api/client.ts` に `ordersApi`, `productsApi`, `uploadApi`, `salesApi` が実装されており、`VITE_API_URL` 環境変数を参照してバックエンドの Flask API と通信します。

- **状態管理 / サイド効果**
    - `@tanstack/react-query` を使用してデータ取得・キャッシュ・invalidate が行われています。

#### ページ

| ページ | ファイル | 説明 | 状態 |
|--------|----------|------|------|
| POS注文画面 | `pages/Orders.tsx` → `components/pos/POSScreen.tsx` | POSレジ風の注文入力画面 | ✅ 実装済み |
| 商品管理 | `pages/Products.tsx` | 商品のCRUD操作 | ✅ 実装済み |

#### コンポーネント

| コンポーネント | ファイル | 説明 | 状態 |
|----------------|----------|------|------|
| POSメイン画面 | `components/pos/POSScreen.tsx` | カテゴリタブ・商品リスト・カート機能 | ✅ **NEW** |
| カートプレビュー | `components/pos/CartPreview.tsx` | カート内容確認ポップアップ | ✅ **NEW** |
| 発注完了ポップアップ | `components/pos/OrderConfirmPopup.tsx` | 発注完了時の確認画面 | ✅ **NEW** |
| 注文一覧ポップアップ | `components/pos/OrdersListPopup.tsx` | 本日の注文一覧・ステータス管理 | ✅ **NEW** |
| 注文カード | `components/orders/OrderCard.tsx` | 個別注文の表示・ステータス変更 | ✅ 実装済み |
| 新規注文フォーム | `components/orders/NewOrderForm.tsx` | 注文作成モーダル（旧） | ⚠️ 非推奨 |
| 商品フォーム | `components/products/ProductForm.tsx` | 商品作成・編集モーダル | ✅ 実装済み |
| 本日の売上 | `components/sales/TodaySales.tsx` | 当日売上サマリーカード | ✅ 実装済み |
| UIコンポーネント | `components/ui/*` | Button, Card, Input, Badge等 | ✅ 実装済み |

#### APIクライアント

| モジュール | 説明 | 状態 |
|------------|------|------|
| `ordersApi` | 注文CRUD操作 | ✅ 実装済み |
| `productsApi` | 商品CRUD操作 | ✅ 実装済み |
| `uploadApi` | ファイルアップロード | ✅ 実装済み |
| `salesApi` | 売上データ取得 | ✅ 実装済み |

### データベース（Supabase）

| テーブル | 説明 | 状態 |
|----------|------|------|
| `products` | 商品マスタ（id, name, image_url, price, is_active, category, description） | ✅ 実装済み |
| `orders` | 注文データ（id, order_number, items, status, notes, total_amount） | ✅ 実装済み |
| RLSポリシー | Row Level Security設定 | ✅ 実装済み |
| Storage | `product_images` バケット | ✅ 実装済み |

---

## 売上計算機能

注文された個数と商品価格に基づいて売上を自動計算します。

| 機能 | 説明 | 優先度 | 状態 |
|------|------|--------|------|
| 📊 当日の売り上げ | 本日の総売上金額をリアルタイム表示 | 🔴 高 | ✅ 実装済み |
| 📈 全期間の総売り上げ | アカウント作成からの累計売上 | 🟡 中 | 🚧 未実装 |
| 📅 期間指定の売り上げ | 〇年○月○日から△年△月△日までの総売上 | 🟡 中 | 🚧 未実装 |

---

## 未実装機能

| 機能 | 説明 | 優先度 |
|------|------|--------|
| 🔐 認証機能 | 店舗ごとのログイン・マルチテナント対応 | 🔴 高 |
| 📈 全期間売上 | 累計売上の表示 | 🟡 中 |
| 📅 期間指定売上 | 日付範囲での売上集計 | 🟡 中 |
| 📊 売上レポート | CSV/PDFエクスポート | 🟢 低 |
| 🔔 通知機能 | 新規注文の通知 | 🟢 低 |

---

## 🎨 UI/UX 未実装機能一覧

> KitchPadデモUIを参考にした、従業員向けPOSレジUIの実装計画
> 
> 📅 **最終更新**: 2026年1月9日

### 📱 ヘッダー・ナビゲーション

| # | 機能 | 説明 | 優先度 | 状態 |
|---|------|------|--------|------|
| 1 | ☰ ハンバーガーメニュー | サイドメニューの開閉 | 🟡 中 | 🚧 未実装 |
| 2 | 🔍 検索バー | 商品名での検索機能 | 🟡 中 | 🚧 未実装 |
| 3 | 🛒 カートアイコン（バッジ付き） | 現在のカート内商品数を表示 | 🔴 高 | ✅ **実装済み** |

### 🍽️ POSレジ画面（注文入力）

| # | 機能 | 説明 | 優先度 | 状態 |
|---|------|------|--------|------|
| 4 | カテゴリタブ | フード / ドリンク / セット等のカテゴリ切り替え | 🟡 中 | ✅ **実装済み** |
| 5 | カテゴリアイコン | 各カテゴリにアイコン表示（🍔🥤等） | 🟢 低 | ✅ **実装済み** |
| 6 | メニュー一覧表示 | 商品を縦リストで表示（画像+名前+説明+価格） | 🔴 高 | ✅ **実装済み** |
| 7 | 合計金額のリアルタイム表示 | 画面下部に常時表示 | 🔴 高 | ✅ **実装済み** |

### 🛒 カート機能

| # | 機能 | 説明 | 優先度 | 状態 |
|---|------|------|--------|------|
| 8 | 数量調整（±ボタン） | 商品カード上で「-1 / 数量 / +1」のコントロール | 🔴 高 | ✅ **実装済み** |
| 9 | 商品メモアイコン | 商品カードから直接メモを追加（カスタマイズ用） | 🟡 中 | 🚧 未実装 |
| 10 | カートプレビュー | カートアイコンタップで小さなポップアップ表示 | 🔴 高 | ✅ **実装済み** |
| 11 | カート全削除 | ワンタップで全削除（確認ダイアログ付き） | 🟡 中 | ✅ **実装済み** |

### 📋 発注・注文管理

| # | 機能 | 説明 | 優先度 | 状態 |
|---|------|------|--------|------|
| 12 | 「発注」ボタン | 「会計」→「発注」に変更、注文確定ボタン | 🔴 高 | ✅ **実装済み** |
| 13 | 発注完了ポップアップ | 発注後に完了メッセージ + 注文番号を表示 | 🔴 高 | ✅ **実装済み** |
| 14 | 発注確認アイコン | ヘッダーに配置、タイムスタンプ一覧をポップアップ表示 | 🔴 高 | ✅ **実装済み** |
| 15 | タイムスタンプカード | 注文ごとのカード（削除可能、確認ダイアログ付き） | 🔴 高 | ✅ **実装済み** |
| 16 | ステータス管理 | 「作成中」→「提供済み」のステータス遷移 | 🔴 高 | ✅ **実装済み** |
| 17 | 注文番号の自動採番 | 当日の連番（#001, #002...）で管理 | 🔴 高 | ✅ **実装済み** |

### 📊 売上表示

| # | 機能 | 説明 | 優先度 | 状態 |
|---|------|------|--------|------|
| 18 | 横スワイプで売上表示 | スワイプで合計売上画面に切り替え | 🟡 中 | 🚧 未実装 |
| 19 | 月別・年別・全期間売上 | 売上画面で期間切り替え | 🟡 中 | 🚧 未実装 |

### 🗄️ データモデル追加

| # | 項目 | 説明 | 優先度 | 状態 |
|---|------|------|--------|------|
| 20 | 商品カテゴリ | `products`テーブルに`category`カラム追加 | 🟡 中 | ✅ 実装済み |
| 21 | 商品説明文 | `products`テーブルに`description`カラム追加 | 🟢 低 | ✅ 実装済み |

---

## 📖 UI/UX機能 詳細仕様

### 🛒 カート機能詳細

#### 8. 数量調整（±ボタン）

```
┌─────────────────────────────────────┐
│ 🍔 ハンバーガー          ¥500      │
│                                     │
│     [ - ]    2    [ + ]             │
│                                     │
│ 📝 メモを追加                       │
└─────────────────────────────────────┘
```

| 項目 | 仕様 |
|------|------|
| 表示タイミング | 商品をカートに追加後 |
| 数量0の動作 | 自動的にカートから削除 |
| ボタンサイズ | 48px × 48px 以上（タップしやすく） |
| 視覚フィードバック | タップ時にボタン色変化 |

#### 9. 商品メモアイコン

| 項目 | 仕様 |
|------|------|
| アイコン | 📝 または ✏️ |
| タップ動作 | 小さなテキスト入力ポップアップを表示 |
| ユースケース | 「辛さ控えめ」「アレルギー対応」「大盛り」等 |
| 文字数制限 | 50文字まで |
| 保存先 | `order.items[].note` フィールド |

#### 10. カートプレビュー

```
┌─────────────────────────────────┐
│ 🛒 カート (3点)           [ × ] │
├─────────────────────────────────┤
│ ハンバーガー      ×2     ¥1,000 │
│ コーラ            ×1       ¥300 │
│ ポテト            ×1       ¥400 │
├─────────────────────────────────┤
│ 合計                     ¥1,700 │
│                                 │
│ [🗑️ 全削除]       [📋 発注]    │
└─────────────────────────────────┘
```

| 項目 | 仕様 |
|------|------|
| 表示方法 | ポップアップ（フルモーダルではない） |
| 閉じ方 | 外側タップ または ×ボタン |
| 編集機能 | 数量変更・削除が可能 |
| 配置 | 画面下部からスライドアップ または 中央ポップアップ |

#### 11. カート全削除（クイックリセット）

| 項目 | 仕様 |
|------|------|
| ボタン | 🗑️ ゴミ箱アイコン（赤系） |
| 確認ダイアログ | 「カートを空にしますか？」→「はい」「いいえ」 |
| 動作 | 全商品を一括削除、合計金額を0にリセット |

---

### 📋 発注・注文管理詳細

#### 12. 「発注」ボタン

| 項目 | 仕様 |
|------|------|
| ボタン名 | 「発注」（「会計」から変更） |
| 配置 | 画面下部、横幅いっぱい |
| 色 | 青系 または オレンジ系（目立つ色） |
| サイズ | 高さ 56px 以上 |
| 動作 | 注文をデータベースに保存、発注完了ポップアップを表示 |

#### 13. 発注完了ポップアップ

```
┌─────────────────────────┐
│                         │
│          ✅             │
│                         │
│      発注完了！         │
│                         │
│    注文番号: #042       │
│                         │
│       [閉じる]          │
│                         │
└─────────────────────────┘
```

| 項目 | 仕様 |
|------|------|
| 表示時間 | 3秒後に自動で閉じる または タップで閉じる |
| 内容 | ✅アイコン + 「発注完了！」+ 注文番号 |
| アニメーション | フェードイン + スケールアップ |

#### 14. 発注確認アイコン

| 項目 | 仕様 |
|------|------|
| アイコン | 📋 または 🕐 |
| 配置 | ヘッダー右側 |
| バッジ | 未提供（作成中）の件数を表示 |
| タップ動作 | タイムスタンプ一覧ポップアップを表示 |

#### 15. タイムスタンプカード

```
┌─────────────────────────────────────┐
│ #042                  🟠 作成中     │
│ 12:34                               │
│ ─────────────────────────────────── │
│ ハンバーガー ×2, コーラ ×1          │
│                                     │
│   [🗑️ 削除]        [✅ 提供完了]   │
└─────────────────────────────────────┘
```

| 項目 | 仕様 |
|------|------|
| 表示情報 | 注文番号、時刻、商品リスト（省略形）、ステータス |
| 削除 | 確認ダイアログ付き（「この注文を削除しますか？」） |
| 提供完了ボタン | タップでステータスを「提供済み」に変更 |

#### 16. ステータス管理

| ステータス | 色 | 説明 |
|------------|-----|------|
| 🟠 作成中 | オレンジ | 調理中・準備中 |
| 🟢 提供済み | 緑 | お客様に提供完了 |
| ⚫ キャンセル | グレー | 注文キャンセル（オプション） |

#### 17. 注文番号の自動採番

| 項目 | 仕様 |
|------|------|
| 形式 | `#001`, `#002`, ... |
| リセット | 毎日0時にリセット（#001から再開） |
| 表示 | 発注完了ポップアップ、タイムスタンプカード、レシート |

---

### 📊 売上表示詳細

#### 18. 横スワイプで売上表示

| 項目 | 仕様 |
|------|------|
| 操作 | 左右スワイプで画面切り替え |
| ページ1 | 注文入力画面（メイン） |
| ページ2 | 売上サマリー画面 |
| インジケーター | ドットで現在のページを表示 |
| 実装方法 | `react-swipeable` または CSS scroll-snap |

#### 19. 月別・年別・全期間売上

```
┌─────────────────────────────────────┐
│  📊 売上サマリー                    │
├─────────────────────────────────────┤
│  [本日] [今月] [今年] [全期間]      │
├─────────────────────────────────────┤
│                                     │
│       ¥125,000                      │
│                                     │
│  注文数: 42件    販売数: 156個      │
│                                     │
└─────────────────────────────────────┘
```

| タブ | 説明 |
|------|------|
| 本日 | 今日の売上（既存の TodaySales コンポーネント） |
| 今月 | 当月1日〜今日までの売上 |
| 今年 | 当年1月1日〜今日までの売上 |
| 全期間 | アカウント作成からの累計売上 |

---

### 📐 UI/UXデザイン方針（KitchPadデモ参考）

| 観点 | 方針 | 具体例 |
|------|------|--------|
| 👁️ 視認性 | 高コントラスト、暗い車内・直射日光下でも見やすい | 白背景 + 青/オレンジのアクセントカラー |
| ⚡ 効率 | 最小タップ数で発注まで完了する動線設計 | 商品タップ → 数量調整 → 発注の3ステップ |
| 📏 余白設計 | 誤タップ防止のための十分な余白 | ボタン間隔 16px 以上 |
| 👆 操作性 | 大きめのボタン配置（濡れた手・汚れた手でも操作可能） | タップ領域 48px × 48px 以上 |
| 🎨 色使い | ステータスを色で直感的に識別 | 作成中=オレンジ、提供済み=緑 |
| 📱 レスポンシブ | スマートフォン縦持ちに最適化 | 片手操作を考慮した配置 |

---

## 技術スタック

- **フロントエンド**: React + TypeScript + Vite
- **バックエンド**: Flask (Python)
- **データベース**: Supabase (PostgreSQL)
- **認証**: Supabase Auth
- **スタイリング**: Tailwind CSS + shadcn/ui
- **状態管理**: TanStack Query

## プロジェクト構造

```
kitchen_my_app/
├── frontend/           # React + TypeScript
│   ├── src/
│   │   ├── api/        # APIクライアント
│   │   ├── components/ # UIコンポーネント
│   │   ├── pages/      # ページコンポーネント
│   │   └── lib/        # ユーティリティ
│   ├── package.json
│   └── tsconfig.json
├── backend/            # Flask + Python
│   ├── app.py
│   ├── requirements.txt
│   ├── supabase_schema.sql
│   └── .env.example
└── README.md
```

## セットアップ手順

### 1. Supabaseのセットアップ

1. [Supabase](https://supabase.com/)でアカウントを作成
2. 新しいプロジェクトを作成
3. SQLエディタで `backend/supabase_schema.sql` を実行
4. Storageで `product-images` バケットを作成（公開設定）
5. プロジェクトのURL と anon key をメモ

### 2. バックエンドのセットアップ

```bash
cd backend

# 仮想環境の作成
python -m venv venv

# 仮想環境の有効化
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# パッケージのインストール
pip install -r requirements.txt

# 環境変数の設定
cp .env.example .env
# .env ファイルを編集してSupabaseの認証情報を設定
```

`.env` ファイルを編集:
```env
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your-anon-key
FLASK_DEBUG=True
PORT=5000
```

### 3. フロントエンドのセットアップ

```bash
cd frontend

# パッケージのインストール
npm install

# 環境変数の設定（必要な場合）
# .env ファイルを作成
echo "VITE_API_URL=http://localhost:5000/api" > .env
```

## 開発サーバーの起動

### バックエンド

```bash
cd backend
# 仮想環境を有効化した状態で
python app.py
```

サーバーが `http://localhost:5000` で起動します。

### フロントエンド

```bash
cd frontend
npm run dev
```

アプリケーションが `http://localhost:3000` で起動します。

## API エンドポイント

### 注文 (Orders)

| メソッド | エンドポイント | 説明 |
|---------|---------------|------|
| GET | `/api/orders` | 注文一覧を取得 |
| POST | `/api/orders` | 新規注文を作成 |
| PUT | `/api/orders/:id` | 注文を更新 |
| DELETE | `/api/orders/:id` | 注文を削除 |

### 商品 (Products)

| メソッド | エンドポイント | 説明 |
|---------|---------------|------|
| GET | `/api/products` | 商品一覧を取得 |
| POST | `/api/products` | 新規商品を作成 |
| PUT | `/api/products/:id` | 商品を更新 |
| DELETE | `/api/products/:id` | 商品を削除 |

### ファイルアップロード

| メソッド | エンドポイント | 説明 |
|---------|---------------|------|
| POST | `/api/upload` | 画像ファイルをアップロード |

### ヘルスチェック

| メソッド | エンドポイント | 説明 |
|---------|---------------|------|
| GET | `/api/health` | サーバー状態を確認 |

## 本番デプロイ（Render + Vercel）

### Step 1: バックエンドを Render にデプロイ

1. [Render](https://render.com/) にログイン（GitHubアカウント連携推奨）
2. **New → Web Service** を選択
3. GitHubリポジトリを接続し、以下を設定:

| 設定項目 | 値 |
|----------|-----|
| Name | `kitchen-app-backend` |
| Root Directory | `backend` |
| Runtime | `Python` |
| Build Command | `pip install -r requirements.txt` |
| Start Command | `gunicorn -w 4 -b 0.0.0.0:$PORT app:app` |
| Instance Type | `Free` |

4. **Environment Variables** に以下を追加:

| 変数名 | 値 |
|--------|-----|
| `SUPABASE_URL` | Supabaseプロジェクトの URL |
| `SUPABASE_KEY` | Supabaseの anon key |
| `FLASK_DEBUG` | `False` |
| `ALLOWED_ORIGINS` | （Step 2でVercelのURLが決まったら設定） |

5. **Create Web Service** をクリック → デプロイ開始
6. デプロイ完了後、RenderのURLをメモ（例: `https://kitchen-app-backend-xxxx.onrender.com`）
7. ヘルスチェックで動作確認: `https://<render-url>/api/health`

### Step 2: フロントエンドを Vercel にデプロイ

1. [Vercel](https://vercel.com/) にログイン（GitHubアカウント連携推奨）
2. **Add New → Project** を選択
3. GitHubリポジトリをインポート
4. 以下を設定:

| 設定項目 | 値 |
|----------|-----|
| Framework Preset | `Vite` |
| Root Directory | `frontend` |
| Build Command | `npm run build` |
| Output Directory | `dist` |

5. **Environment Variables** に以下を追加:

| 変数名 | 値 |
|--------|-----|
| `VITE_API_URL` | `https://<render-url>/api`（Step 1のRender URL） |

6. **Deploy** をクリック → デプロイ開始
7. デプロイ完了後、VercelのURL（例: `https://kitchen-app-xxxx.vercel.app`）をメモ

### Step 3: CORSの設定

1. Render のダッシュボードに戻る
2. **Environment** タブで `ALLOWED_ORIGINS` を追加:
   - 値: `https://kitchen-app-xxxx.vercel.app`（Step 2のVercel URL）
3. Renderが自動で再デプロイされるのを待つ

### デプロイ後の確認

- フロントエンド: `https://<vercel-url>` にアクセス
- バックエンド: `https://<render-url>/api/health` で `{"status": "healthy"}` を確認
- 商品一覧が表示されること、注文が作成できることを確認

> ⚠️ **注意**: Renderの無料プランはアクティビティがない場合15分後にスリープします。初回アクセス時に30秒〜1分程度の起動時間がかかります。

## 認証機能（Supabase Auth）

複数店舗がこのアプリを利用する場合、Supabase Authを使用して店舗ごとにデータを分離できます。

### アーキテクチャ

```
┌─────────────────────────────────────────────────┐
│              Supabase Auth                       │
│  ・メール/パスワード認証                          │
│  ・Google/GitHub OAuth対応                       │
│  ・RLSで自動的にデータ分離                        │
└─────────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────────┐
│                  Supabase DB                     │
│                                                  │
│  products (user_idで店舗を識別)                   │
│  orders (user_idで店舗を識別)                     │
│                                                  │
│  → 各店舗は自分のデータのみ閲覧・編集可能          │
└─────────────────────────────────────────────────┘
```

### 1. Supabaseで認証を有効化

1. Supabaseダッシュボード → **Authentication** → **Providers**
2. **Email** を有効化（デフォルトで有効）
3. 必要に応じて Google, GitHub などのOAuthを設定

### 2. データベーススキーマの変更

SQLエディタで以下を実行して、既存テーブルに `user_id` を追加：

```sql
-- 既存テーブルにuser_idカラムを追加
ALTER TABLE products ADD COLUMN user_id UUID REFERENCES auth.users(id);
ALTER TABLE orders ADD COLUMN user_id UUID REFERENCES auth.users(id);

-- 既存のRLSポリシーを削除
DROP POLICY IF EXISTS "Allow all on products" ON products;
DROP POLICY IF EXISTS "Allow all on orders" ON orders;
DROP POLICY IF EXISTS "Allow public read access on products" ON products;
DROP POLICY IF EXISTS "Allow public insert on products" ON products;
DROP POLICY IF EXISTS "Allow public update on products" ON products;
DROP POLICY IF EXISTS "Allow public delete on products" ON products;
DROP POLICY IF EXISTS "Allow public read access on orders" ON orders;
DROP POLICY IF EXISTS "Allow public insert on orders" ON orders;
DROP POLICY IF EXISTS "Allow public update on orders" ON orders;
DROP POLICY IF EXISTS "Allow public delete on orders" ON orders;

-- 新しいRLSポリシー（自分のデータのみアクセス可能）
CREATE POLICY "Users can view their own products"
ON products FOR SELECT USING (auth.uid() = user_id);

CREATE POLICY "Users can insert their own products"
ON products FOR INSERT WITH CHECK (auth.uid() = user_id);

CREATE POLICY "Users can update their own products"
ON products FOR UPDATE USING (auth.uid() = user_id);

CREATE POLICY "Users can delete their own products"
ON products FOR DELETE USING (auth.uid() = user_id);

CREATE POLICY "Users can view their own orders"
ON orders FOR SELECT USING (auth.uid() = user_id);

CREATE POLICY "Users can insert their own orders"
ON orders FOR INSERT WITH CHECK (auth.uid() = user_id);

CREATE POLICY "Users can update their own orders"
ON orders FOR UPDATE USING (auth.uid() = user_id);

CREATE POLICY "Users can delete their own orders"
ON orders FOR DELETE USING (auth.uid() = user_id);
```

### 3. フロントエンドに認証機能を追加

```bash
cd frontend
npm install @supabase/supabase-js
```

**Supabaseクライアントの設定** (`src/lib/supabase.ts`):

```typescript
import { createClient } from '@supabase/supabase-js';

const supabaseUrl = import.meta.env.VITE_SUPABASE_URL;
const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY;

export const supabase = createClient(supabaseUrl, supabaseAnonKey);
```

**環境変数の追加** (`frontend/.env`):

```env
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_API_URL=http://localhost:5000/api
```

### 4. 認証API例

**新規登録**:
```typescript
const { data, error } = await supabase.auth.signUp({
  email: 'store@example.com',
  password: 'securepassword'
});
```

**ログイン**:
```typescript
const { data, error } = await supabase.auth.signInWithPassword({
  email: 'store@example.com',
  password: 'securepassword'
});
```

**ログアウト**:
```typescript
await supabase.auth.signOut();
```

**現在のユーザー取得**:
```typescript
const { data: { user } } = await supabase.auth.getUser();
```

### 5. バックエンドでの認証トークン検証

APIリクエストにJWTトークンを含め、バックエンドで検証：

```python
from supabase import create_client
import jwt

def verify_token(token):
    try:
        # Supabaseの公開鍵で検証
        decoded = jwt.decode(token, options={"verify_signature": False})
        return decoded.get('sub')  # user_id
    except:
        return None

@app.before_request
def authenticate():
    if request.endpoint in ['health_check']:
        return
    
    auth_header = request.headers.get('Authorization')
    if auth_header and auth_header.startswith('Bearer '):
        token = auth_header.split(' ')[1]
        user_id = verify_token(token)
        if user_id:
            request.user_id = user_id
            return
    
    return jsonify({'error': 'Unauthorized'}), 401
```

### 6. Flask認証ミドルウェアの詳細実装

認証機能を追加する場合、Flaskにミドルウェアを実装して全APIリクエストを検証します。

#### リクエストの流れ

```
┌─────────────────────────────────────────────────────────────┐
│                      リクエストの流れ                         │
│                                                             │
│  フロントエンド                                               │
│       │                                                     │
│       │ Authorization: Bearer <JWT token>                   │
│       ▼                                                     │
│  ┌─────────────────────────────────────┐                    │
│  │     Flask ミドルウェア（認証）         │ ← ここで検証       │
│  │  1. トークンを検証                    │                   │
│  │  2. user_id を取得                   │                   │
│  │  3. 無効なら 401 エラー               │                   │
│  └─────────────────────────────────────┘                    │
│       │                                                     │
│       │ user_id を付与してリクエスト続行                       │
│       ▼                                                     │
│  ┌─────────────────────────────────────┐                    │
│  │     Flask API エンドポイント          │                   │
│  │  request.user_id でフィルタリング     │                   │
│  └─────────────────────────────────────┘                    │
│       │                                                     │
│       ▼                                                     │
│    Supabase DB（RLSでも二重チェック）                         │
└─────────────────────────────────────────────────────────────┘
```

#### 完全な認証ミドルウェア実装 (`backend/middleware/auth.py`)

```python
import jwt
import os
from functools import wraps
from flask import request, jsonify, g

# 認証が不要なエンドポイント
PUBLIC_ENDPOINTS = [
    'health_check',
    'auth_login',
    'auth_register',
    'static'
]

def get_jwt_secret():
    """Supabase JWT Secretを取得"""
    return os.environ.get('SUPABASE_JWT_SECRET')

def verify_supabase_token(token):
    """
    Supabase JWTトークンを検証してユーザーIDを返す
    """
    try:
        # 署名検証なしでデコード（開発用）
        # 本番環境では署名検証を有効にすること
        decoded = jwt.decode(
            token, 
            options={"verify_signature": False}
        )
        
        # トークンの有効期限をチェック
        import time
        if decoded.get('exp', 0) < time.time():
            return None, "Token expired"
        
        user_id = decoded.get('sub')
        if not user_id:
            return None, "Invalid token: no user_id"
        
        return user_id, None
        
    except jwt.ExpiredSignatureError:
        return None, "Token expired"
    except jwt.InvalidTokenError as e:
        return None, f"Invalid token: {str(e)}"
    except Exception as e:
        return None, f"Token verification failed: {str(e)}"

def require_auth(f):
    """
    認証が必要なエンドポイント用デコレータ
    """
    @wraps(f)
    def decorated_function(*args, **kwargs):
        auth_header = request.headers.get('Authorization')
        
        if not auth_header:
            return jsonify({'error': 'Authorization header missing'}), 401
        
        if not auth_header.startswith('Bearer '):
            return jsonify({'error': 'Invalid authorization format'}), 401
        
        token = auth_header.split(' ')[1]
        user_id, error = verify_supabase_token(token)
        
        if error:
            return jsonify({'error': error}), 401
        
        # ユーザーIDをグローバルコンテキストに保存
        g.user_id = user_id
        
        return f(*args, **kwargs)
    
    return decorated_function

def init_auth_middleware(app):
    """
    Flaskアプリに認証ミドルウェアを追加
    """
    @app.before_request
    def authenticate():
        # 公開エンドポイントはスキップ
        if request.endpoint in PUBLIC_ENDPOINTS:
            return None
        
        # OPTIONSリクエスト（CORS preflight）はスキップ
        if request.method == 'OPTIONS':
            return None
        
        auth_header = request.headers.get('Authorization')
        
        if not auth_header or not auth_header.startswith('Bearer '):
            return jsonify({'error': 'Unauthorized'}), 401
        
        token = auth_header.split(' ')[1]
        user_id, error = verify_supabase_token(token)
        
        if error:
            return jsonify({'error': error}), 401
        
        # リクエストにユーザーIDを付与
        g.user_id = user_id
        
        return None
```

#### app.pyへの統合

```python
# backend/app.py

import os
from datetime import datetime
from flask import Flask, request, jsonify, g
from flask_cors import CORS
from supabase import create_client, Client
from werkzeug.utils import secure_filename
from dotenv import load_dotenv
import uuid

# ミドルウェアをインポート
from middleware.auth import init_auth_middleware

load_dotenv()

app = Flask(__name__)
CORS(app)

# 認証ミドルウェアを初期化
init_auth_middleware(app)

# Supabase設定
SUPABASE_URL = os.environ.get('SUPABASE_URL')
SUPABASE_KEY = os.environ.get('SUPABASE_KEY')
supabase: Client = create_client(SUPABASE_URL, SUPABASE_KEY)

# === 認証 API ===

@app.route('/api/auth/register', methods=['POST'])
def auth_register():
    """新規ユーザー登録"""
    data = request.get_json()
    email = data.get('email')
    password = data.get('password')
    
    if not email or not password:
        return jsonify({'error': 'Email and password required'}), 400
    
    try:
        response = supabase.auth.sign_up({
            'email': email,
            'password': password
        })
        return jsonify({
            'user': response.user.model_dump() if response.user else None,
            'session': response.session.model_dump() if response.session else None
        }), 201
    except Exception as e:
        return jsonify({'error': str(e)}), 400

@app.route('/api/auth/login', methods=['POST'])
def auth_login():
    """ログイン"""
    data = request.get_json()
    email = data.get('email')
    password = data.get('password')
    
    if not email or not password:
        return jsonify({'error': 'Email and password required'}), 400
    
    try:
        response = supabase.auth.sign_in_with_password({
            'email': email,
            'password': password
        })
        return jsonify({
            'user': response.user.model_dump() if response.user else None,
            'session': response.session.model_dump() if response.session else None
        })
    except Exception as e:
        return jsonify({'error': str(e)}), 401

@app.route('/api/auth/me', methods=['GET'])
def auth_me():
    """現在のユーザー情報を取得"""
    return jsonify({'user_id': g.user_id})

# === 注文 API（user_idでフィルタリング） ===

@app.route('/api/orders', methods=['GET'])
def get_orders():
    """自分の注文一覧を取得"""
    try:
        response = supabase.table('orders') \
            .select('*') \
            .eq('user_id', g.user_id) \
            .order('created_date', desc=True) \
            .execute()
        
        return jsonify(response.data)
    except Exception as e:
        return jsonify({'error': str(e)}), 500

@app.route('/api/orders', methods=['POST'])
def create_order():
    """新規注文を作成（自動的にuser_idを付与）"""
    try:
        data = request.get_json()
        
        order_data = {
            'user_id': g.user_id,  # ← 認証されたユーザーIDを自動付与
            'order_number': data.get('order_number', str(uuid.uuid4())[:8]),
            'items': data.get('items'),
            'status': data.get('status', 'pending'),
            'notes': data.get('notes', ''),
            'created_date': datetime.utcnow().isoformat()
        }
        
        response = supabase.table('orders').insert(order_data).execute()
        return jsonify(response.data[0]), 201
    except Exception as e:
        return jsonify({'error': str(e)}), 500

# 他のエンドポイントも同様に g.user_id を使用
```

#### フロントエンドでのトークン送信

```typescript
// src/api/client.ts

import { supabase } from '@/lib/supabase';

async function getAuthHeaders(): Promise<HeadersInit> {
  const { data: { session } } = await supabase.auth.getSession();
  
  if (session?.access_token) {
    return {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${session.access_token}`
    };
  }
  
  return {
    'Content-Type': 'application/json'
  };
}

async function request<T>(endpoint: string, options: RequestInit = {}): Promise<T> {
  const url = `${API_BASE_URL}${endpoint}`;
  const headers = await getAuthHeaders();
  
  const config: RequestInit = {
    headers: {
      ...headers,
      ...options.headers,
    },
    ...options,
  };

  const response = await fetch(url, config);
  
  if (response.status === 401) {
    // 未認証の場合はログインページにリダイレクト
    window.location.href = '/login';
    throw new Error('Unauthorized');
  }
  
  if (!response.ok) {
    const error = await response.json().catch(() => ({ message: 'API Error' }));
    throw new Error(error.message || `HTTP error! status: ${response.status}`);
  }

  return response.json();
}
```

#### 環境変数の追加

`backend/.env`:
```env
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your-anon-key
SUPABASE_JWT_SECRET=your-jwt-secret  # Settings > API > JWT Secret
FLASK_DEBUG=True
PORT=5000
```

## ライセンス

MIT
